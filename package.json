{
  "name": "my-hashtag-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "lighthouse": "npm run build && npx --yes concurrently --kill-others --success first \"vite preview --port 4174\" \"npx --yes wait-on http://localhost:4174 && lighthouse http://localhost:4174 --output html --output-path ./lighthouse-report.html --chrome-flags='--headless=new' --view\"",
    "lighthouse:json": "npm run build && npx --yes concurrently --kill-others --success first \"vite preview --port 4174\" \"npx --yes wait-on http://localhost:4174 && lighthouse http://localhost:4174 --output json --output-path ./lighthouse-report.json --chrome-flags='--headless=new'\""
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.2.0",
    "lighthouse": "^13.0.3",
    "tailwindcss": "^4.2.0",
    "typescript": "~5.9.3",
    "vite": "^8.0.0-beta.13"
  },
  "overrides": {
    "vite": "^8.0.0-beta.13"
  },
  "dependencies": {
    "@huggingface/transformers": "^3.8.1",
    "web-vitals": "^5.1.0"
  }
}
